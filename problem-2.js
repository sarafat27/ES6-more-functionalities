// const devide = x => x / 5;
// console.log(devide(38));

// const calculate = (x, y) => (x + 2) * (y + 2);
// console.log(calculate(5, 4));

const calculate = (x, y) => {
    const add = x + 2;
    const add2 = y + 2;
    const result = add * add2;
    return result;
}
console.log(calculate(5, 4));
const x = _ => console.log(345);
x();